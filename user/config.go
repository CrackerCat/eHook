package user

const PackageName = ""

const LibraryName = ""

// The offset to which the onEnter function will be attached. 0 if not used.
const Enter_Offset = 0

// The offset to which the onLeave function will be attached. 0 if not used.
const Leave_Offset = 0